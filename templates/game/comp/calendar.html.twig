
{# 
    <pre>
        {{ dump() }}
    </pre> #}
    <div class="row">
     Calendar:
        <table class="table">
        <caption >September</caption>
        <thead>
            <tr>
            <th scope="col">Mon</th>
            <th scope="col">Tue</th>
            <th scope="col">Wed</th>
            <th scope="col">Thu</th>
            <th scope="col">Fri</th>
            <th scope="col">Sat</th>
            <th scope="col">Sun</th>
            </tr>
        </thead>
        <tbody>
            {% for  row in calendarArray %}
                <tr id="">
                {% for day in row %}
                    <td id="{{day.date|date("Y-m-d")}}" class="{{day.status}}">{{day.number}}
                        {% if day.status == 'VOTED' %}
                            --><b>{{day.playersLeftToVote}}</b> 
                        {% endif %}
                        {% if day.availableHours %}
                            {% for match in day.availableHours %}
                                |{{match.0}}-{{match.1}}
                            {% endfor %}
                        {% endif %}
                    </td>
                    {% endfor %}
                </tr>
            {% endfor %}
        </tbody>
        </table>
    </div>